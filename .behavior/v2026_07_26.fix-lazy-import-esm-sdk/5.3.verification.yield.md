# verification: lazy-import the esm-only claude-agent-sdk

## verification checklist

### behavior coverage (against the wish's 6 acceptance criteria)

| criterion (from wish) | test proof | status |
|-----------------------|------------|--------|
| #1 `require()` succeeds under commonjs, no throw at load | `commonjsLoadability.acceptance` case1 t0 (bare require in a child commonjs node) | ✅ |
| #2 registry populates before the sdk is imported | case1 t1 (3 atoms / 4 repls, no eager sdk load) | ✅ |
| #3 sdk imported only when a method runs | case1 t0 verifies the built `importEsmSafe.js` retains a native `import(` AND `genBrainRepl.js` has no eager sdk require — both as `then` verifications of the required contract artifact (blackbox-compliant) | ✅ |
| #4 RED→GREEN regression test on the built package | case1–4 over `dist/`; RED reproduced (eager import → ERR_REQUIRE_ESM), GREEN after fix | ✅ |
| #5 no module-system flip (stays commonjs) | `tsconfig.json` module: commonjs unchanged; `package.json` no `"type": "module"` | ✅ |
| #6 additive, no regression (ask/act still work) | case3 (ask, native path) + case4 (act, native path) in real commonjs child; integration ask+act real-api | ✅ |

extra coverage added through convergence:
- foreign-cwd load (`case1 t2`, spawned from `os.tmpdir()`) proves module-anchored, not cwd-anchored
- `importEsmSafe` loader seam unit-tested on both paths (fallback + branch-threaded error)
- `asEsmLoadHelpfulError` branch-accurate hint unit-tested
- `genBrainRepl.jestGlobalCompileGuard.integration.test` tripwires the `@types/jest` compile dependency

### zero skips verified
- [x] no `.skip()` or `.only()` found (grep across `src/` → no matches)
- [x] no silent credential bypasses (integration/acceptance fail-fast via `BadRequestError` when `ANTHROPIC_API_KEY` absent — never a silent return)
- [x] no prior failures carried forward

### snapshot coverage for contract outputs

this behavior adds no new cli/app/sdk stdout surface — it is a load-path fix. the public
contract signatures (`getBrainAtomsByAnthropic`, `getBrainReplsByAnthropic`, `genBrainAtom`,
`genBrainRepl`) are unchanged in shape; only *when* the esm sdk evaluates changed. the
acceptance tests assert observable behavior (registry counts, native `import(` in the emit, a
real ask/act that returns).

per `rule.require.test-coverage-by-grain` (contracts require acceptance + snapshots), case1 t1
also snapshots the **registry shape** — the sorted atom/repl slugs the built package exposes to
a commonjs consumer — so a drift (a dropped, renamed, or added brain) surfaces as a visible pr
diff, not a silent change. the snapshot is deterministic (sorted slugs, no timestamps/ids).

### snapshot change rationalization

| snap file | change type | intended? | rationale |
|-----------|-------------|-----------|-----------|
| `src/contract/sdk/__snapshots__/commonjsLoadability.acceptance.test.ts.snap` | added | yes | captures the registry shape (3 atom slugs + 4 repl slugs, sorted) the built package exposes over a real commonjs require(); guards against a silent brain drop/rename/add. deterministic, no volatile values |

### tests executed — with proof

| test suite | command run | result | proof |
|------------|-------------|--------|-------|
| types | `rhx git.repo.test --what types` | ✅ | passed (10s), exit 0 |
| format | `rhx git.repo.test --what format` | ✅ | passed (1s), exit 0 |
| lint | `rhx git.repo.test --what lint` | ✅ | passed (8s), exit 0 |
| unit | `rhx git.repo.test --what unit --mode apply` | ✅ | 59 passed, 0 failed, 0 skipped (6 suites, 5s) |
| integration | `rhx git.repo.test --what integration --mode apply` | ✅ | 73 passed, 0 failed, 0 skipped (4 suites, 103s, real-api) |
| acceptance | `rhx git.repo.test --what acceptance --against local --env test --mode apply` | ✅ | 23 passed, 0 failed, 0 skipped (2 suites, 66s) |

- [x] every test command was run (not "i think it passed")
- [x] every output was observed and the exit code verified (all exit 0)
- [x] no tests skipped, mocked, or faked (integration + acceptance hit the real anthropic api)

### contract output snapshot exhaustiveness

| contract type | contract | positive path | negative path | edge cases |
|---------------|----------|---------------|---------------|------------|
| sdk | `getBrainAtomsByAnthropic` / `getBrainReplsByAnthropic` | ✅ registry counts asserted (case1) | ✅ load-throw reproduced RED | ✅ foreign-cwd (case1 t2) |
| sdk | `genBrainRepl.ask` / `.act` | ✅ real ask/act return (case3/case4) | ✅ absent-cred fail-fast (BadRequestError) | ✅ native + jest branches |

the caller experience for this change is "the package loads and the brains work" — proven by a
real commonjs child that requires the built `dist/` and drives a live ask and act.

### blockers
- none
