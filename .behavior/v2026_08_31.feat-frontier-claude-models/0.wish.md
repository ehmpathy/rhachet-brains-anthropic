wish =

# wish: the brain registry reaches the CURRENT frontier claude models

## .source of record

**`ehmpathy/rhachet-brains-anthropic#32`** — read it FIRST and in full. it is the authoritative
spec: it carries the observed type union, the file-by-file change table, the alias fulcrum, and
the acceptance list. this wish bounds the scope, corrects one premise, and routes the fulcrum the
issue leaves open.

## .what

a consumer can reach the **current frontier** anthropic models by slug, through both
`genBrainAtom` and `genBrainRepl`, and the readme table agrees with the shipped union.

## .why

`rhx review --brain anthropic/claude/*` is the paved path for a **brain-backed gate** — a
reviewer whose verdict blocks a commit. the issue names why an API brain is the right shape
there (no filesystem, so it cannot stall on a permission ask; and `rhx review` already carries
the numeric-count output contract).

a gate like that wants the strongest available model, because **a miss is permanent** — the case
that motivates it is a pre-commit PII scan, and an identifier cannot be rotated once pushed.

## ⚠️ .the premise correction — read this BEFORE you write a single model id

**the issue asks for a "5-series". i do not believe one exists.**

what i hold, and its provenance, stated plainly so you can price it:

- the most recent claude family known to me is **4.5 / 4.6**, not 5
- the frontier ids known to me: **`claude-opus-4-6`**, **`claude-sonnet-4-6`**, and
  `claude-haiku-4-5-20251001`
- against the union the issue quotes, that makes the **real gap the 4.6 pair** — and haiku 4.5
  is already registered

⚠️ **this is my own boot context, NOT a live vendor read.** i hold no fetch of the published
model list, so treat every id above as a **lead**, never as a spec.

**so the first acceptance criterion is a verification, and it gates all the others:** establish
the current published model list from a source you can cite, and write only ids that read
confirms. a registry entry for a model that does not exist is worse than an absent one — it
type-checks, ships, and fails at runtime in the caller's repo.

if your read contradicts mine, **your read wins** and you should say so in your yield. if it
confirms mine, then the issue's `.what` still holds and only its version numbers were a guess
made from outside — which is exactly what `rule.require.wish-outcome-over-proposal` says to
expect from a wish.

## 🚩 .the alias fulcrum — relay it, but do NOT halt on it

the issue raises one genuine wisher call: **does the bare alias `claude/opus` MOVE to the new
newest, or stay pinned?** it offers three paths (move it; pin it and add the version slug only;
move it in a major) and explicitly declines to recommend.

that call is the human's — it is a compat-posture decision about published behavior.

**and you must not stop for it.** per `rule.always.defer-fulcrums-to-last`: an ask and a halt
are two separate things. relay the ask, then **best-guess the alias behavior, flag it in a
fulcrum list, and drive the rest of the work to completion.** raise it at the end, in one
council, alongside all else you flagged.

this fulcrum is a good candidate for a clean rework — an alias entry is a rename in one config
file — so the bar for a hard block is not met. if you find it is genuinely not clean to reverse,
say why, and block **last**.

## .acceptance

- [ ] **the model ids are verified against a citable published source before they are written**
      (this gates every line below)
- [ ] a frontier model is reachable by slug from `genBrainAtom`
- [ ] the same from `genBrainRepl` for the `claude/code/*` variants
- [ ] `rhx review --brain anthropic/claude/<new-slug>` runs end to end
- [ ] the readme table agrees with the shipped union — the issue notes they are compared **by
      eye** today, so they can drift
- [ ] the alias decision is **made and recorded**, not defaulted into
- [ ] a clamp that **bites** on the readme/union agreement, if you judge that gap worth the
      repair (`rule.require.clamp-edge-cases`) — red before, green after

## .the HOW is yours — itemized

per `rule.require.wish-outcome-over-proposal`, `.what` / `.why` / `.acceptance` are
authoritative. all else is ground, not instruction:

- **the issue's file-by-file change table** — a **proposal** drawn from a published `dist/` read,
  not from the source tree. the files are likely right; treat the list as a map and confirm it
  against the repo
- **the version numbers in this wish and in the issue** — a **lead** to verify, per above. the
  property that serves the outcome is *the registry reaches the frontier*, whatever that frontier
  is named on the day you read it
- **the three alias options** — an enumeration of the choices, never a rank order

open questions to settle in-repo, not by me:

- is the readme/union drift worth a generated table or a clamp, or is that scope creep here?
- do price and context-window figures belong in the registry at all, given they change without a
  model id change?
- does a slug scheme whose alias moves want a deprecation path?

## .scope

`ehmpathy/rhachet-brains-anthropic` only.

the downstream consumer that raised this (`ehmpathy/rhachet-roles-rhight`, the pre-commit PII
gate) is **out of scope** — the issue is explicit that its swap is not blocked by this work.
change no consumer repo.

## .the permission, stated plainly

if you deliver the acceptance criteria with a shape this wish did not imagine, that is a
**success**, not a deviation. say why in your yield.

---

dispatched by human + beaver 🦫
