# self-review: has-pruned-backcompat

## review scope

reviewed code changes for backwards compatibility concerns:
- `src/domain.operations/atoms/genBrainAtom.ts`

## analysis

### signature changes

| change | backwards compatible? | reason |
|--------|----------------------|--------|
| `plugs?: TPlugs` added | ✅ yes | optional parameter |
| `prompt: AsBrainPromptFor<TPlugs>` | ✅ yes | accepts string when no plugs |
| `calls` in output | ✅ yes | additive field |
| `output` can be null | ⚠️ breaks compat | previously always returned value |

### compat break identified

**`output` can now be `null`**

when model returns tool calls instead of final answer, `output` is `null`.

this is intentional — the behavior change matches the vision:
- tools require a loop: ask → tool calls → execute → continue
- final output comes only when model completes

**is this backcompat concern requested?**

no explicit backcompat was requested. the blueprint states:
> "output and calls are independent (not mutually exclusive)"

this implies the caller must handle both states.

### backcompat shims in code?

none found. no:
- deprecated parameters
- fallback behaviors
- version checks
- legacy code paths

## verdict

✅ no unnecessary backcompat code

the one compat break (`output` can be `null`) is intentional per the design.
callers must update to handle the tool use workflow.
