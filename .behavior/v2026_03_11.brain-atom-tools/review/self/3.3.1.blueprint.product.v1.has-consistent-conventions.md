# review: has-consistent-conventions

## reflection

searched for extant name patterns via grep. examined each new name to verify alignment with conventions.

---

## extant name conventions identified

### verb prefixes

| prefix | usage | purpose | location |
|--------|-------|---------|----------|
| `gen*` | `genBrainAtom`, `genBrainRepl` | factory functions | domain.operations/*.ts |
| `as*` | `asJsonSchema` | type conversion | infra/schema/*.ts |
| `cast*` | `castBriefsToPrompt` | format conversion | rhachet import |
| `translate*` | `translateHookToClaudeCode` | domain translation | hooks/translateHook.ts |
| `parse*` | `JSON.parse` | JSON parse | built-in |
| `calc*` | `calcBrainOutputCost` | calculation | rhachet import |

### boolean prefixes

| prefix | usage | pattern |
|--------|-------|---------|
| `is*` | `isToolResultContinuation` | boolean state |
| `has*` | `hasToolCalls` | boolean existence |

---

## new names reviewed

### 1. parseExchangeContent

**location:** `genBrainAtom.ts:110`

**question:** does this follow extant conventions?

**analysis:** the function parses JSON-serialized content blocks. the `parse` prefix is appropriate for JSON parse operations. alternatives considered:
- `castExchangeContent` — implies type change, but this parses JSON
- `asContentBlocks` — implies conversion, but this is conditional parse

the `parse` prefix is accurate: it parses JSON and returns the parsed result or the original string.

**verdict:** consistent. `parse` is appropriate for JSON parse operations.

---

### 2. isToolResultContinuation

**location:** `genBrainAtom.ts:93`

**code:** `const isToolResultContinuation = Array.isArray(askInput.prompt);`

**question:** does this follow extant conventions?

**analysis:** `is*` prefix for booleans is standard. the name describes what is checked: whether the prompt is a tool result continuation (array of executions).

**verdict:** consistent.

---

### 3. hasToolCalls

**location:** `genBrainAtom.ts:173`

**code:** `const hasToolCalls = toolUseBlocks.length > 0;`

**question:** does this follow extant conventions?

**analysis:** `has*` prefix for boolean existence checks is standard.

**verdict:** consistent.

---

### 4. toolUseBlocks

**location:** `genBrainAtom.ts:161`

**question:** does this follow extant conventions?

**analysis:** `*Blocks` suffix matches anthropic's terminology (`ToolUseBlock`, `TextBlock`). the name describes what it contains.

**verdict:** consistent with anthropic SDK terminology.

---

### 5. currentPromptContent

**location:** `genBrainAtom.ts:96`

**question:** does this follow extant conventions?

**analysis:** descriptive variable name. `current*` prefix indicates this is the prompt processed now. `*Content` suffix matches anthropic's `MessageParam['content']` type.

**verdict:** consistent with anthropic SDK terminology.

---

### 6. priorMessages

**location:** `genBrainAtom.ts:123`

**question:** does this follow extant conventions?

**analysis:** `prior*` prefix indicates these are messages from before the current exchange. follows noun_adj pattern (messages that are prior).

**verdict:** consistent.

---

## cross-system name mapping

### slug ↔ name translation

**location:** `genBrainAtom.ts:142, 182`

**code:**
```ts
// line 142: rhachet → anthropic
name: tool.slug,

// line 182: anthropic → rhachet
slug: block.name,
```

**analysis:** rhachet uses `slug` for tool identifiers. anthropic uses `name`. the implementation correctly translates between them:
- outbound (to anthropic): `tool.slug` → `name`
- inbound (from anthropic): `block.name` → `slug`

**verdict:** correct translation. this mapping is documented in the vision and is consistent with rhachet's contract.

---

## structure consistency check

| pattern | extant usage | new usage | consistent? |
|---------|--------------|-----------|-------------|
| helper inside function | `queryResult` extraction in genBrainRepl | `parseExchangeContent` in ask() | yes |
| inline const | throughout | `isToolResultContinuation`, etc. | yes |
| type guards | `block.type === 'tool_use'` | same pattern | yes |
| cross-system name mapping | `translateHookToClaudeCode` maps fields | `slug` ↔ `name` mapping | yes |

---

## terms alignment check

| rhachet term | anthropic term | mapping correct? |
|--------------|----------------|------------------|
| `slug` | `name` | yes (lines 142, 182) |
| `exid` | `id` | yes (lines 100, 181) |
| `BrainPlugToolInvocation` | `ToolUseBlock` | yes (lines 179-184) |
| `BrainPlugToolExecution` | `ToolResultBlockParam` | yes (lines 97-106) |

---

## conclusion

all new names follow extant conventions. cross-system term translation (`slug` ↔ `name`, `exid` ↔ `id`) is correct and consistent with rhachet's contract. boolean prefixes (`is*`, `has*`) are standard. anthropic SDK terminology (`*Block`, `*Content`) is reused correctly.

