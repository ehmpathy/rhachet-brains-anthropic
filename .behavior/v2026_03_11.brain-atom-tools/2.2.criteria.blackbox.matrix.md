# blackbox criteria matrix: brain atom tools

## matrix.1 = tool invocation

| ind: prompt requires tools | dep: output | dep: calls |
|---------------------------|-------------|------------|
| yes                       | null        | invocations with exid, slug, input |
| no                        | defined     | null       |

no gaps — all combinations covered.

## matrix.2 = tool result continuation

| ind: signal | dep: brain behavior |
|-------------|---------------------|
| success     | uses output value   |
| error:constraint | reasons about violation, adjusts approach |
| error:malfunction | reasons about failure, reports or retries |

no gaps — all signal types covered.

## matrix.3 = output schema with tools

| ind: schema present | dep: output type |
|---------------------|------------------|
| yes                 | typed, conforms to schema |
| no                  | unstructured text |

no gaps — both schema modes covered.

## matrix.4 = tool call cardinality

| ind: tools needed | dep: calls.tools | dep: exid |
|-------------------|------------------|-----------|
| single            | 1 invocation     | unique    |
| multiple          | N invocations    | each unique |

no gaps — single and parallel covered.

## matrix.5 = continuation state

| ind: brain satisfied | dep: output | dep: calls |
|---------------------|-------------|------------|
| yes                 | defined     | null       |
| no                  | null        | new invocations |

no gaps — continuation loop terminates correctly.

---

## decomposition analysis

all matrices have 1-2 independent dimensions — well-scoped behavioral boundaries.

no decomposition needed.

