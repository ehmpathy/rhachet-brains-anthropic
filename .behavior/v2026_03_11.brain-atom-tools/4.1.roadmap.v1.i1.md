# roadmap: brain atom tools

## overview

implement tool use support for `genBrainAtom` with supplier pattern corrections.

**note**: base implementation exists from prior session (45/45 tests pass). this roadmap covers:
1. extract cast operations to dedicated files
2. apply independent extraction pattern (output + calls not mutually exclusive)
3. verify tests pass with corrections applied

---

## phase 0: verify baseline

**read before**:
- `.behavior/v2026_03_11.brain-atom-tools/3.1.3.research.internal.product.code.test._.v1.md` (if declared)

**checklist**:
- [ ] run `npm run test:integration -- genBrainAtom.integration.test.ts`
- [ ] confirm 45/45 tests pass (baseline)
- [ ] note any failed tests before corrections

**acceptance**: all extant tests pass

---

## phase 1: create cast operations

**read before**:
- `.behavior/v2026_03_11.brain-atom-tools/3.3.1.blueprint.product.v1.i1.md` (implementation notes section)

**checklist**:
- [ ] create `src/infra/cast/` directory
- [ ] create `src/infra/cast/castIntoAnthropicToolDef.ts`
  - input: `BrainPlugToolDefinition`
  - output: `Anthropic.Messages.Tool`
  - maps: `slug` → `name`, `description`, `schema.input` → `input_schema`
- [ ] create `src/infra/cast/castIntoAnthropicToolResult.ts`
  - input: `BrainPlugToolExecution`
  - output: `{ type: 'tool_result', tool_use_id, content, is_error }`
  - maps: `exid` → `tool_use_id`, `output` → JSON content, `signal` → `is_error`
- [ ] create `src/infra/cast/castFromAnthropicToolUse.ts`
  - input: `Anthropic.Messages.ToolUseBlock`
  - output: `BrainPlugToolInvocation`
  - maps: `id` → `exid`, `name` → `slug`, `input` → `input`
- [ ] run `npm run test:types` — confirm no type errors

**acceptance**: 3 cast files exist with correct signatures, types check pass

---

## phase 2: refactor genBrainAtom to use cast operations

**read before**:
- `.behavior/v2026_03_11.brain-atom-tools/3.3.1.blueprint.product.v1.i1.md` (codepath tree)

**checklist**:
- [ ] import cast operations into `genBrainAtom.ts`
- [ ] replace inline tool definition translation (lines 140-147) with `castIntoAnthropicToolDef`
- [ ] replace inline tool result construction (lines 98-105) with `castIntoAnthropicToolResult`
- [ ] replace inline tool invocation extraction (lines 179-184) with `castFromAnthropicToolUse`
- [ ] run `npm run test:integration -- genBrainAtom.integration.test.ts`

**acceptance**: tests pass, inline transformations replaced with cast operations

---

## phase 3: apply independent extraction pattern

**read before**:
- `.behavior/v2026_03_11.brain-atom-tools/3.3.1.blueprint.product.v1.i1.md` (independent extraction pattern)

**checklist**:
- [ ] refactor output extraction (lines 172-176)
  - extract calls from tool_use blocks FIRST (independent)
  - extract text block (independent)
  - try to parse output from text
  - if parse fails AND calls present → output = null (graceful fallback)
  - if parse fails AND no calls → throw error
- [ ] verify output and calls can BOTH be present simultaneously
- [ ] run `npm run test:integration -- genBrainAtom.integration.test.ts`

**acceptance**: tests pass, output and calls extracted independently

---

## phase 4: verify full test suite

**read before**:
- `.behavior/v2026_03_11.brain-atom-tools/2.1.criteria.blackbox.md`

**checklist**:
- [ ] run `npm run test:types`
- [ ] run `npm run test:unit`
- [ ] run `npm run test:integration`
- [ ] confirm all tests pass

**acceptance**: full test suite green

---

## phase 5: commit

**checklist**:
- [ ] verify `git.commit.uses get` has quota
- [ ] stage all changes
- [ ] commit with message that describes:
  - tool use support for brain atoms
  - cast operations at provider boundary
  - independent extraction pattern

**acceptance**: changes committed

---

## dependencies

```
phase 0 (baseline)
    │
    v
phase 1 (cast operations)
    │
    v
phase 2 (refactor to use casts)
    │
    v
phase 3 (independent extraction)
    │
    v
phase 4 (verify tests)
    │
    v
phase 5 (commit)
```

---

## verification matrix

| phase | verification command | expected |
|-------|---------------------|----------|
| 0 | `npm run test:integration -- genBrainAtom` | 45/45 pass |
| 1 | `npm run test:types` | no errors |
| 2 | `npm run test:integration -- genBrainAtom` | 45/45 pass |
| 3 | `npm run test:integration -- genBrainAtom` | 45/45 pass |
| 4 | `npm run test:integration` | all pass |
| 5 | `git status` | clean |

